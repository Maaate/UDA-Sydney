'use strict';

/**
 * @ngdoc function
 * @name udaApp.controller:TimetableTimetableCtrl
 * @description
 * # TimetableTimetableCtrl
 * Controller of the udaApp
 */
angular.module('udaApp')
  .controller('TimetableTimetableCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
